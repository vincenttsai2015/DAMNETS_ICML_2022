# DAMNETS 家族執行手冊

在台灣杉二號（`ln01.twcc.ai`）上跑 DAMNETS / AGE / DYMOND 三個模型，
資料是注入 burst / hysteresis 巨觀動態的多層時序圖序列。

## 分工

一人負責一個 seed，互不干擾。

| seed | 負責人 | array index |
|---|---|---|
| 0 | 奕諾 | 0~63 |
| 1 | 永結 | 64~127 |
| 2 | 嘉憲 | 128~191 |

以下把 `<S>` 換成自己的 seed 編號。

**TagGen 已從 baseline 移除**——它對每一條序列各自訓練一個 transformer，
superuser 一組要 24 小時以上，佔了整批九成以上的時間。現在只跑
DAMNETS / AGE / DYMOND 三個。要加回來的話設 `BASELINE_MODELS="DAMNET AGE TagGen DYMOND"`。

config 與腳本都在 repo 裡，`git pull` 就會拿到最新的。
**不要自己改**——要調整的話改 repo 再讓大家 pull，這樣三個 seed 的設定才一致。

queue 名額是各帳號獨立的 20 個，腳本預設就是 20，不用調。

---

## A. 環境（只做一次）

```bash
ssh <帳號>@ln01.twcc.ai
```

```bash
module load miniconda3/conda24.5.0_py3.9
source "$(conda info --base)/etc/profile.d/conda.sh"
conda create -y -n damnets python=3.10
conda activate damnets
```

```bash
python -m pip install --upgrade pip
python -m pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu124
python -m pip install torch_geometric
python -m pip install networkx networkx-temporal pandas numpy scipy scikit-learn matplotlib tqdm pyyaml easydict tensorboard
python -m pip install pyemd pygsp gensim python-igraph msgpack powerlaw
```

`pyemd` 編譯失敗的話改用 `conda install -c conda-forge pyemd`。

### 驗證

```bash
cd ~/DAMNETS_ICML_2022 && python env_check_damnets.py
```

**要看到**：19 個套件全部 `OK`、`torch path` 落在 `~/.conda/envs/damnets/...`、
`cuda avail: True`。任何一項不對就停下來處理，後面都會失敗。

## B. 取得程式碼

```bash
cd ~ && git clone --depth 1 https://github.com/vincenttsai2015/DAMNETS_ICML_2022.git && cd DAMNETS_ICML_2022 && mkdir -p logs/slurm
```

### 驗證

```bash
cd ~/DAMNETS_ICML_2022 && ls run_macro_damnets.sh submit_macro.sh check_baselines.sh check_eval.sh collect_fails.sh && ls experiment_configs/macro_*.yaml | wc -l
```

**要看到**：五個腳本都在，config 數量是 **64**
（32 個組合 × `gnn` / `age` 兩個模型各一份）。

## C. 取得資料

資料是 2.9 GB，不在 repo 裡（GitHub 單檔上限 100 MB，我們最大的檔 185 MB）。
向奕諾取得後放到 `~/DAMNETS_ICML_2022/data/macro/`。

### 驗證

```bash
cd ~/DAMNETS_ICML_2022 && ls data/macro/nx_macro_*.pkl | wc -l && du -sh data/macro
```

**要看到**：**40** 個檔案、約 **2.9 GB**。

```bash
python -c "
import pickle
g=pickle.load(open('data/macro/nx_macro_wiki_vote_burst_L3_support.pkl','rb'))
print(len(g), '條序列，每條', len(g[0]), '張，第一張', g[0][0])"
```

**要看到**：序列數、每條 **32** 張、以及類似 `Graph with 158 nodes and 48 edges`。

---

## D. 先跑一組

確認整條流程通得了再送全部。

```bash
cd ~/DAMNETS_ICML_2022 && bash run_macro_damnets.sh --list | awk -v S=<S> '$NF==S' | head -3
```

**要看到**：三行，最後一欄都是 `<S>`。

```bash
sbatch --array=$((64*<S>)) run_macro_damnets.sh
squeue -u $USER
```

```bash
tail -30 ~/DAMNETS_ICML_2022/logs/slurm/dmn_macro_*_$((64*<S>)).out
```

**要看到**：`===== TRAIN =====` 之後訓練有進展、最後有 `===== DONE`，
沒有 Traceback。

## E. 全部送出

```bash
cd ~/DAMNETS_ICML_2022 && SEEDS="<S>" bash submit_macro.sh
```

**要看到**：印出的組合清單最後一欄全部是 `<S>`。不是的話停下來——
表示 `SEEDS` 沒生效，送出去會跟別人衝突。

queue 空出來就再執行同一條，重複到它說沒有待跑的。

```bash
squeue -u $USER | wc -l
```

## F. 看進度

```bash
cd ~/DAMNETS_ICML_2022 && for d in ../test_and_generated_graphs/macro_*_<S>; do k=${d##*/}; s=""; for m in DAMNET AGE DYMOND; do [ -f "$d/$m/sampled_ts.pkl" ] && s="$s O" || s="$s -"; done; printf '%-50s %s\n' "$k" "$s"; done
```

三欄依序是 DAMNET / AGE / DYMOND。

## G. 補跑

array job 只保證 DAMNETS 與 AGE。DYMOND 掛在同一個 task 後面，
它失敗時 array job 仍會被視為完成，要單獨補。

```bash
cd ~/DAMNETS_ICML_2022 && SEED_TAG=<S> bash check_baselines.sh
```

**要看到**：組合名稱都以 `_<S>` 結尾。確認清單沒問題再送：

```bash
SEED_TAG=<S> bash check_baselines.sh --submit
```

送不完的等 queue 空再執行同一條。重複到「有缺 0 組」。

### 失敗時

```bash
cd ~/DAMNETS_ICML_2022 && bash collect_fails.sh
```

會寫出 `~/fails.tar.gz`，內含所有非正常結束的 job 與各自的 log 尾段。
抓回來給奕諾，程式碼要改的話會 push 到同一個分支，之後 `git pull` 取得。

## H. 算 MMD / KS

三個模型都齊的組合才能算。

```bash
cd ~/DAMNETS_ICML_2022 && SEED_TAG=<S> bash check_eval.sh
```

**要看到**：「可以算 32 組」。還有「等 baseline 補跑」的就回到 G。

```bash
SEED_TAG=<S> MMD_MAX_SEQS=36 bash check_eval.sh --submit
```

`MMD_MAX_SEQS=36` **不可以改**。MMD 是 O(序列數²)，各資料集的序列數差很多
（wiki-vote 36~38 條、twitter 266~282 條、superuser 635~680 條），
配對數差 357 倍。36 是所有組合的最小值，取樣數不同就沒有可比性。

跑完收成 CSV：

```bash
cd ~/DAMNETS_ICML_2022 && python collect_metrics.py --csv > ~/macro_mmd_ks_seed<S>.csv && wc -l < ~/macro_mmd_ks_seed<S>.csv
```

**要看到**：**97** 行（32 組 × 3 個模型 + 標題列）。

## I. 打包回傳

```bash
cd ~ && find test_and_generated_graphs -maxdepth 3 -name 'sampled_ts.pkl' -path "*_<S>/*" ! -path "*/TagGen/*" > /tmp/pack.txt && find test_and_generated_graphs -maxdepth 3 -name 'test_graphs.pkl' -path "*_<S>/DAMNET/*" >> /tmp/pack.txt && echo macro_mmd_ks_seed<S>.csv >> /tmp/pack.txt && wc -l < /tmp/pack.txt && du -ch $(cat /tmp/pack.txt) 2>/dev/null | tail -1
```

**要看到**：**129** 個檔案（32×3 個生成結果 + 32 個原始序列 + 1 個 CSV）。

從自己的電腦串回來，伺服器端不落地：

```bash
ssh <帳號>@ln01.twcc.ai "cd ~ && tar czf - -T /tmp/pack.txt" > macro_seed<S>.tar.gz
```

## J. 清理

DYMOND 對每一條序列各開一個子目錄放中間檔。
家目錄配額是 100 GB，滿了會出現 `OSError: [Errno 122] Disk quota exceeded`，
而且是在跑到一半時才爆。

```bash
du -ch ~/test_and_generated_graphs/*/*/train_edgelists 2>/dev/null | tail -1
```

檔案數很多，`rm` 會跑很久，丟背景：

```bash
nohup rm -rf ~/test_and_generated_graphs/*/*/train_edgelists > /dev/null 2>&1 &
```

```bash
du -sh ~/test_and_generated_graphs
```
