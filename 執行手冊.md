# DAMNETS 家族執行手冊

在晶創26（`nano4.nchc.org.tw`）上跑 DAMNETS / AGE / DYMOND。

## 分工

| seed | array index |
|---|---|
| 0 | 0~63 |
| 1 | 64~127 |
| 2 | 128~191 |

以下把 `<S>` 換成自己的 seed 編號。

config 與腳本都在 repo 裡，不要自己改——要調整的話改 repo 再讓大家 pull。

---

## A. 環境

`--account` 是 `acd109125`。

```bash
ssh <帳號>@nano4.nchc.org.tw
```

```bash
cd ~ && wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh && bash Miniconda3-latest-Linux-x86_64.sh -b -p ~/miniconda3 && ~/miniconda3/bin/conda init bash && source ~/.bashrc
```

```bash
conda create -y -n damnets python=3.10 && conda activate damnets
```

H200 是 sm_90，torch 要 cu121 以上：

```bash
PIP_USER=0 pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu121
```

```bash
PIP_USER=0 pip install torch_geometric
PIP_USER=0 pip install networkx networkx-temporal pandas numpy scipy scikit-learn matplotlib tqdm pyyaml easydict tensorboard
PIP_USER=0 pip install pyemd pygsp gensim python-igraph msgpack powerlaw
```

`pyemd` 編譯失敗改用 `conda install -c conda-forge pyemd`。
`PIP_USER=0` 防止套件落到 `~/.local` 蓋過 conda env。

之後每次登入：

```bash
conda activate damnets
```

### 驗證

要在運算節點跑，登入節點禁止計算：

```bash
cd ~/DAMNETS_ICML_2022 && srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/damnets/bin/python env_check_damnets.py
```

**要看到**：19 個套件全部 `OK`、`torch path` 在 `~/miniconda3/envs/damnets/...`、
`cuda avail: True`。

```bash
srun --account=acd109125 --partition=dev --nodes=1 --gres=gpu:1 --cpus-per-task=12 --time=00:10:00 ~/miniconda3/envs/damnets/bin/python -c "import torch; print(torch.cuda.get_device_name(0), torch.cuda.get_device_capability(0))"
```

**要看到**：**NVIDIA H200** 與 **(9, 0)**。

---

## B. 程式碼

```bash
cd ~ && git clone https://github.com/vincenttsai2015/DAMNETS_ICML_2022.git && cd DAMNETS_ICML_2022 && mkdir -p logs/slurm
```

### 驗證

```bash
cd ~/DAMNETS_ICML_2022 && ls run_macro_damnets.sh run_baselines.sh submit_macro.sh check_baselines.sh collect_fails.sh check_broken.py && ls experiment_configs/macro_*.yaml | wc -l && grep -c "fit_frames" run_dymond.py && grep -c "DYMOND_OBS" run_macro_damnets.sh
```

**要看到**：六個腳本都在、config **64** 個、最後兩個數字不是 0。

```bash
cd ~/DAMNETS_ICML_2022 && grep -E "^#SBATCH --(account|partition|gres|cpus|mem)" run_macro_damnets.sh
```

**要看到**：`acd109125`、`8gpus`、`gpu:1`、`12`、`200G`。

---

## C. 資料

安裝 rclone：

```bash
curl -O https://downloads.rclone.org/rclone-current-linux-amd64.zip && unzip -q rclone-current-linux-amd64.zip && mkdir -p ~/bin && cp rclone-*/rclone ~/bin/ && chmod +x ~/bin/rclone && echo 'export PATH="$HOME/bin:$PATH"' >> ~/.bashrc && export PATH="$HOME/bin:$PATH"
```

`rclone.conf` 放到 `~/.config/rclone/rclone.conf` 並 `chmod 600`。

```bash
rclone listremotes
```

**要看到**：`gdrive:`。

下載並解開（588 MB，解開後 5.8 GB）：

```bash
cd ~ && rclone copy --drive-root-folder-id 1wBCissrBmxsVubagKFIvzQoRR83snwLP gdrive:baseline_data_new.tar.gz . -P && tar xzf baseline_data_new.tar.gz -C DAMNETS_ICML_2022/data
```

`data/macro` 給 DAMNETS 與 AGE、`data/macro_obs` 給 DYMOND，兩個都要。

### 驗證

```bash
cd ~/DAMNETS_ICML_2022 && echo "target $(ls data/macro/*.pkl | wc -l) 個，觀測窗 $(ls data/macro_obs/*.pkl | wc -l) 個" && ~/miniconda3/envs/damnets/bin/python -c "
import pickle
a=pickle.load(open('data/macro/nx_macro_wiki_vote_burst_L3_support.pkl','rb'))
b=pickle.load(open('data/macro_obs/nx_macro_wiki_vote_burst_L3_support.pkl','rb'))
print(f'target {len(a)} 條 x {len(a[0])} 張  {a[0][0]}')
print(f'觀測窗 {len(b)} 條 x {len(b[0])} 張')"
```

**要看到**：各 **40** 個、`target 375 條 x 17 張`、`觀測窗 375 條 x 16 張`。

---

## D. 先跑一組

```bash
cd ~/DAMNETS_ICML_2022 && bash run_macro_damnets.sh --list | awk -v S=<S> '$NF==S' | head -3
```

**要看到**：三行，最後一欄都是 `<S>`。

```bash
sbatch --array=$((64*<S>)) run_macro_damnets.sh && squeue --me
```

跑完：

```bash
tail -30 ~/DAMNETS_ICML_2022/logs/slurm/dmn_macro_*_$((64*<S>)).out
```

**要看到**：`===== TRAIN =====` 之後有進展、
`觀測窗: data/macro_obs/nx_macro_...pkl`、最後 `===== DONE`，沒有 Traceback。

出現 `[WARN] ... 找不到` 回 C 段。

### 驗證形狀

```bash
cd ~/DAMNETS_ICML_2022 && D=$(ls -d ../test_and_generated_graphs/macro_*_<S> | head -1) && ~/miniconda3/envs/damnets/bin/python -c "
import pickle,numpy as np
D='$D'
for tag,p in [('參考','DAMNET/test_graphs.pkl'),('DAMNET','DAMNET/sampled_ts.pkl'),('DYMOND','DYMOND/sampled_ts.pkl')]:
    try:
        g=pickle.load(open(f'{D}/{p}','rb'))
        e=[x.number_of_edges() for s in g for x in s]
        print(f'{tag:8s} {len(g):5d} 條 x {len(g[0]):2d} 張  每張邊數 {np.mean(e):6.1f}  空圖 {100*sum(1 for x in e if x==0)/len(e):.1f}%')
    except FileNotFoundError: print(f'{tag:8s} 還沒有')"
```

**要看到**：DAMNET **17 張**、DYMOND **16 張**。
DYMOND 也是 17 張的話回 C 段確認 `data/macro_obs/`。

---

## E. 全部送出

先看會送什麼、同時跑幾個：

```bash
cd ~/DAMNETS_ICML_2022 && DRY=1 SEEDS="<S>" bash submit_macro.sh | tail -4
```

**要看到**：組合清單最後一欄全部是 `<S>`、`同時執行 N 個` 的 N ≥ 1。
不是的話停下來。

依資料集分批送，wiki_vote 的併發最寬：

```bash
SEEDS="<S>" ONLY=wiki_vote bash submit_macro.sh
```

```bash
SEEDS="<S>" ONLY=twitter bash submit_macro.sh
```

```bash
SEEDS="<S>" ONLY=superuser bash submit_macro.sh
```

queue 空出來就再執行同一條，重複到說沒有待跑的。

```bash
squeue --me | wc -l && df -h ~ | tail -1
```

**要看到**：家目錄剩餘不低於 12 GB。低於的話停下來跑 I 段。

superuser 的併發會被算到很低（一組就要 26 GB）。先把 wiki_vote 與 twitter 跑完、
清掉 `experiment_files`，再送 superuser。

log 出現 `[WAIT] 剩餘 ...G < 需要 ...G` 表示那個 task 在等空間，正常。
出現 `[ERROR] 等了 3600s` 表示等不到就自己退出了，那組要清完殘留再重送。

---

## F. 看進度

```bash
cd ~/DAMNETS_ICML_2022 && for d in ../test_and_generated_graphs/macro_*_<S>; do k=${d##*/}; s=""; for m in DAMNET AGE DYMOND; do [ -f "$d/$m/sampled_ts.pkl" ] && s="$s O" || s="$s -"; done; printf '%-50s %s\n' "$k" "$s"; done
```

三欄依序是 DAMNET / AGE / DYMOND。

---

## G. 補跑

```bash
cd ~/DAMNETS_ICML_2022 && SEED_TAG=<S> bash check_baselines.sh
```

**要看到**：組合名稱都以 `_<S>` 結尾。

```bash
SEED_TAG=<S> bash check_baselines.sh --submit
```

送不完的等 queue 空再執行同一條，重複到「有缺 0 組」。

### 單獨補一組 DYMOND

`run_baselines.sh` 有 SBATCH 標頭，**要用 sbatch 送**，直接 `bash` 會在登入節點跑。

```bash
cd ~/DAMNETS_ICML_2022 && DYMOND_OBS=data/macro_obs/nx_macro_superuser_raw_L0_a2q.pkl sbatch --export=ALL run_baselines.sh macro_superuser_a2q_raw <S> dymond
```

檔名對照 `nx_macro_<資料集>_<mode>_L<層編號>_<層名>.pkl`：
`superuser_a2q` L0、`c2a` L1、`c2q` L2；`twitter_MT` L0、`RT` L2；
`wiki_vote_neutral` L0、`oppose` L2、`support` L3。

### 失敗時

```bash
cd ~/DAMNETS_ICML_2022 && bash collect_fails.sh
```

寫出 `~/fails.tar.gz`，抓回來回報。

---

## H. 打包回傳

指標由 `eval_all_metrics.py` 事後統一算。

```bash
cd ~ && find test_and_generated_graphs -maxdepth 3 -name 'sampled_ts.pkl' -path "*_<S>/*" ! -path "*/TagGen/*" > /tmp/pack.txt && find test_and_generated_graphs -maxdepth 3 -name 'test_graphs.pkl' -path "*_<S>/DAMNET/*" >> /tmp/pack.txt && wc -l < /tmp/pack.txt && du -ch $(cat /tmp/pack.txt) 2>/dev/null | tail -1
```

**要看到**：**128** 個檔案。

```bash
cd ~ && tar czf macro_seed<S>.tar.gz -T /tmp/pack.txt && rclone copy macro_seed<S>.tar.gz --drive-root-folder-id 1xSqJQVs7E7WuMnYHB1ca3gTp4Z0hKkeO gdrive: -P
```

上傳失敗顯示 `invalid_grant: maybe token expired` 的話，是 OAuth token 過期，
要在本機 `rclone config reconnect gdrive:` 之後把 `rclone.conf` 重新傳過來。

---

## I. 清理

```bash
du -sh ~/DAMNETS_ICML_2022/experiment_files ~/test_and_generated_graphs 2>/dev/null && df -h ~ | tail -1
```

跑完的組合產出都在 `test_and_generated_graphs`，`experiment_files` 可以整個清：

```bash
cd ~ && nohup rm -rf DAMNETS_ICML_2022/experiment_files > /dev/null 2>&1 &
```

DYMOND 對每一條序列各開一個子目錄放中間檔：

```bash
nohup rm -rf ~/test_and_generated_graphs/*/*/train_edgelists > /dev/null 2>&1 &
```

```bash
df -h ~ | tail -1
```

### 磁碟寫爆過的話

截斷的檔案會被當成已完成而跳過，要先掃出來刪掉才會重跑。

```bash
cd ~/DAMNETS_ICML_2022 && ~/miniconda3/envs/damnets/bin/python check_broken.py
```

**要看到**：每個檔一行 `OK` 加上形狀，壞的會標 `壞` 與原因。

出現 `[ERROR] ... 環境問題` 表示 python 用錯了——產出裡是 networkx 的圖，
要用 damnets 環境的 python，不是 base 的。

確認清單無誤再刪：

```bash
cd ~/DAMNETS_ICML_2022 && ~/miniconda3/envs/damnets/bin/python check_broken.py --delete
```

只會刪掉壞檔所在的模型目錄（`DAMNET/` 或 `AGE/` 或 `DYMOND/`），
同組合的其他模型保留。刪完之後 E 段重送就會補回來。
